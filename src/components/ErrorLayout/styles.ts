import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  centerLayout: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  errorText: {
    marginBottom: 7,
  },
});
